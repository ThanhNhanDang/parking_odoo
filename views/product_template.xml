<record model="ir.ui.view" id="product_template_kanban_view_extened">
    <field name="name">res.partner.kanban.view.extened</field>
    <field name="inherit_id" ref="product.product_template_kanban_view" />
    <field name="model">product.template</field>
    <field name="arch" type="xml">

        <div name="product_lst_price" class="mt-1"> Chủ sở hữu: <field
                name="responsible_id" widget="many2one_avatar_user"
                options="{'currency_field': 'currency_id', 'field_digits': True}" />
        </div>
    </field>
</record>